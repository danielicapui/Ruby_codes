def entrada()
    t=[]
    dia,n = gets.split.map(&:to_s)
    t.push(a)
    return t
end
def processamento(dia,n)
    

    return saida(area,v)
end

def saida(area,v)
        if v==1
            puts("Perimetro = #{area.round(1)}")
        end
        if v==0
            puts("Area = #{area.round(1)}")
        end
        
end

a=entrada()

rabbit=processamento(a[0],a[1])

